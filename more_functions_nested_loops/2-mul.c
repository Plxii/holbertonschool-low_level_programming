#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplie deux nombres entiers.
 * @a: Le paramètre
 * @b: Le paramètre
 *
 * Return: int
 */
int mul(int a, int b)
{
	if (a * b && a * b > 0)
	{
		return (a * b);
	}
	else
	{
		return (a * b);
	}
}
