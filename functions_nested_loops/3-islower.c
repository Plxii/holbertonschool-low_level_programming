#include "main.h"
#include <ctype.h>

/**
 * int _islower(int c) - ecrire une fonction qui verifie la présence de caractère minuscules.
 *
 * Return: Always 0.
 */

int _islower(int c);
{
	int c:
		char x:

		for (x = 'A'; x <= 'Z'; c++)
			for (c = 'a'; c <= 'z'; c++)
			{
				if (c == 'a')
				{

					_putchar('1');
				}
				else
				{

					_putchar('0');
				}
			}
}
